---
description: Explain a system design concept clearly, with tradeoffs and when to use it
---

Use the `system-design` skill. The user wants concept `$ARGUMENTS` explained.

If the explanation depends on the user's context (their stack, scale, or use case), ask with `AskUserQuestion` first — offer options, let them pick or answer "Other" — then tailor the explanation. If the concept is context-free, explain directly.

Load the matching file under `skills/system-design/references/` and explain:
1. What it is, in plain terms (one short paragraph).
2. When you'd reach for it — and when you wouldn't.
3. The tradeoff it makes.
4. One concrete example.

Keep it tight. Offer a diagram only if the concept is spatial/flow-based.
