---
description: Produce a quick-reference cheatsheet for a system design area
---

Use the `system-design` skill. Area: `$ARGUMENTS`.

If the area, or the depth/format the user wants, is ambiguous, clarify with `AskUserQuestion` first (which topic, how deep, plain sheet vs saved file).

Pull the matching reference file and condense it into a scannable cheatsheet: key choices, when-to-use table, failure modes, and numbers worth knowing. Optimize for glance-ability, not prose. If the user asks to save it, write it as a markdown file.
