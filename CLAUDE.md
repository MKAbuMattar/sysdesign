# sysdesign

Agent instructions for this repo live in **@AGENTS.md** — read it first. It covers what
this plugin is, the layout, the self-contained-content rule, the working commands
(validate JSON, verify no external links), and the hard rules (valid manifests, zero
external guide links, clean conventional commits, don't commit local tooling dirs).

## Authoring context

- **@DESIGN.md** — the editorial system: the voice, the reference-file structure, the
  formatting rules (colon definition lists, tables, em-dash discipline, tabular density),
  and the absolute bans (no external links in the skill, no copied ByteByteGo text or
  images, no AI tells).

Keep this file thin: put durable instructions in `AGENTS.md`, not here.
